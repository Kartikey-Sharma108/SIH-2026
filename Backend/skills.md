Packages: express, cors, dotenv, pg, node-fetch
Endpoints: POST /api/analyze (main gateway), GET /api/logs, GET /api/logs/:id
Core modules: heuristics.js (regex/keyword check), riskEngine.js (score aggregation), guardrail.js (Prompt Guard API call), llmProxy.js (pass-through call)
Prompt for Claude Code:
"Build a Node.js + Express backend for an AI Security Gateway. Use pg for Postgres, node-fetch for external API calls, dotenv for config, and cors. Project structure:- server.js — Express app entry point- routes/analyze.js — POST /api/analyze- routes/logs.js — GET /api/logs, GET /api/logs/:id- lib/heuristics.js — Layer 1: regex + keyword checks against known_attacks table (ILIKE match)- lib/guardrail.js — Layer 2: calls Hugging Face Inference API running Meta's Prompt Guard model, returns { label: "SAFE"|"INJECTION"|"JAILBREAK", confidence: number }- lib/riskEngine.js — aggregates score: base 0, +40 if regex/keyword match, +50 if similarity match, +70 if guardrail flags unsafe, capped at 100- lib/llmProxy.js — Layer 3/4: wraps safe prompts in <user_input> XML delimiters, calls [Claude/Gemini] API, does a basic regex scan on the response for PII (phone numbers, emails, API-key-shaped strings) before returning POST /api/analyze behavior: 1. Accept { prompt: string } in the body 2. Run heuristics check 3. Run guardrail check (async, but await it — this is a demo, prioritize correctness over max speed) 4. Compute risk score via riskEngine 5. If score > 70: do NOT call the LLM. Insert a row into security_logs with action_taken="blocked", block_reason describing which layer(s) triggered, trigger_words populated. Return { blocked: true, riskScore, reason, triggerWords } 6. If score <= 70: call llmProxy, insert a row with action_taken="allowed", return { blocked: false, riskScore, response } GET /api/logs — return the 50 most recent security_logs rows, newest first GET /api/logs/:id — return a single row with full detail Include error handling: if the guardrail API times out (>3s) or errors, treat as risk_score += 30 and continue rather than crashing. Log the failure mode clearly to console for demo debugging. Add a .env.example with DATABASE_URL, HUGGINGFACE_API_KEY, LLM_API_KEY, RISK_THRESHOLD=70."