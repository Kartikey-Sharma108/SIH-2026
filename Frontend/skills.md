Packages: react, vite, axios, recharts (for the risk score chart), lucide-react (icons)
Components needed: LogTable, IncidentDetail, PromptInput, RiskBadge
State: just useState/useEffect + polling — skip Redux, no time for it
Prompt for Claude Code:
"Build a React + Vite dashboard for an AI Security Gateway demo. Use axios for API calls, recharts for a small risk score visual, lucide-react for icons. No Redux — use useState/useEffect only. Pages/Components: 1. App.jsx — layout with a PromptInput at top, LogTable below, IncidentDetail as a slide-over panel 2. PromptInput.jsx- Textarea + "Analyze" button- Posts to POST /api/analyze- Shows a loading spinner during the ~12s guardrail call- On response, shows an inline result banner: green "Passed — Risk: X" or red "Blocked — Risk: X, Reason: Y" 3. LogTable.jsx- Polls GET /api/logs every 2 seconds- Columns: timestamp, truncated prompt, risk score (as RiskBadge), action (allowed/blocked), click row to open IncidentDetail- Use a subtle flash/highlight animation when a new row appears 4. RiskBadge.jsx- Color-coded pill: green if score < 40, yellow if 40-70, red if > 70- Show the numeric score inside the pill 5. IncidentDetail.jsx- Slide-over panel triggered by clicking a log row- Fetches GET /api/logs/:id- Shows the full original prompt with trigger_words highlighted inline (wrap matched substrings in a <mark> with red background)- Shows a breakdown: heuristic contribution, guardrail contribution, total score- Shows block_reason if blocked Styling: dark theme, monospace font for prompt text, clean security-dashboard aesthetic — think a SIEM tool, not a consumer app. Use Tailwind CSS via CDN import, no build config needed. Wire the API base URL to import.meta.env.VITE_API_URL, defaulting to http://localhost:3001."